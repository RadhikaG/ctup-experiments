This app evaluates batched FK (intrin backend) with all optimizations turned on for different robots.

* Files in gen/ are generated by `samples/batched_fk_sweep.cpp`
* Full generation script is: [todo]
